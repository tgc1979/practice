#include <iostream>
#include <stdio.h>
#include "valuable.h"
#include "safe_depo.h"

/* int main () {

  Valuable * item = new Valuable("key",10);
  Valuable item2("phone",2);
  item->print();
  item2.print();

  SafeDeposit safe(3,7,10);
  safe.print();
  if(safe.add(*item))
    {
      safe.print();
      delete item;
      item=nullptr;
    }

    if(safe.add(item2))
   {
      safe.print();
    }
  return 0;
  } */
/*  */